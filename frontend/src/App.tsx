// App.tsx is intentionally minimal — all routing lives in AppRouter.tsx
// This file exists only so Vite's default template references are satisfied.
import AppRouter from './routes/AppRouter'
export default function App() { return <AppRouter /> }
